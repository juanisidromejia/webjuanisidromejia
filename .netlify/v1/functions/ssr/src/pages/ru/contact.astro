---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { getTranslations } from '../../utils/i18n';

const currentLocale = Astro.currentLocale || 'ru';
const translations = await getTranslations(currentLocale);
---

<BaseLayout title={translations.contact_title} description={translations.contact_text} showHeader={false}>
    <article class="mb-16 sm:mb-24">
        <header class="mb-8">
            <h1 class="text-3xl leading-tight font-serif font-medium sm:text-5xl sm:leading-tight">{translations.contact_title}</h1>
            <p class="mt-4 text-lg text-muted">{translations.contact_subtitle}</p>
        </header>
        <div class="max-w-none prose prose-dante sm:prose-lg">
            <p>{translations.contact_text}</p>

            <h2>Электронная почта</h2>
            <p>Не стесняйтесь написать мне на <a href="mailto:juanisidromejia@gmail.com">juanisidromejia@gmail.com</a>, и я постараюсь ответить как можно скорее.</p>

            <h2>Социальные сети</h2>
            <p>Также вы можете связаться со мной в социальных сетях. Найдите меня в <a href="https://instagram.com/" target="_blank" rel="noopener">Instagram</a> или <a href="https://linkedin.com/" target="_blank" rel="noopener">LinkedIn</a>.</p>
        </div>
    </article>
</BaseLayout>